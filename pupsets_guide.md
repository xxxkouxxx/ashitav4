# Pupsets アドオン 操作ガイド

からくり師（PUP）のアタッチメントセットをスラッシュコマンドで管理するアドオン。

## 基本情報

- アドオン名: `pupsets`
- コマンドエイリアス: `/pupsets` / `/pupset` / `/ps`
- セット保存先: `<Ashita>/config/addons/pupsets/*.txt`
- パケット送信モード: `safe`（デフォルト、最低1秒間隔）

---

## コマンド一覧

| コマンド | 説明 |
|---|---|
| `/pupsets help` | ヘルプ表示 |
| `/pupsets list` | 保存済みセットファイル一覧 |
| `/pupsets load <ファイル名>` | セットをロード（オートマトン停止中のみ） |
| `/pupsets save <ファイル名>` | 現在のセットを保存 |
| `/pupsets delete <ファイル名>` | セットファイルを削除 |
| `/pupsets clear` | 全アタッチメントをリセット（`reset` / `unset` も同義） |
| `/pupsets set <スロット> <ID>` | 指定スロットにアタッチメントをIDで設定 |
| `/pupsets setn <スロット> <名前>` | 指定スロットにアタッチメントを名前で設定 |
| `/pupsets delay <秒数>` | パケット送信間隔を変更（safeモード時は最低1秒） |

---

## セットファイルのフォーマット

1行1アイテム、全14行（`.txt`形式）：

```
1行目:  ヘッド名
2行目:  フレーム名
3行目:  アタッチメント スロット1
4行目:  アタッチメント スロット2
...
14行目: アタッチメント スロット12
```

空行 = そのスロットは未装着。

---

## スロット番号

| スロット番号 | 部位 |
|---|---|
| 1 | ヘッド |
| 2 | フレーム |
| 3 〜 14 | アタッチメント（1〜12番） |

---

## 使用例

```
# セットを保存
/ps save myset01

# セットをロード（オートマトン収納後に実行）
/ps load myset01

# 全スロットをリセット
/ps clear

# スロット3に名前でアタッチメントをセット
/ps setn 3 Inhibitor II

# スロット3にIDでセット
/ps set 3 42

# パケット遅延を0.65秒に変更（fastモード想定）
/ps delay 0.65
```

---

## 注意事項

- **ロード前にオートマトンを収納すること。** 展開中はロード不可。
- セットファイルは日本語クライアントで保存した場合、**Shift-JIS** エンコードになる。
- `safe` モードではパケット送信間隔が最低1秒に制限される。
- ファイル名の `.txt` 拡張子は省略可能（コマンド側で自動付与）。
